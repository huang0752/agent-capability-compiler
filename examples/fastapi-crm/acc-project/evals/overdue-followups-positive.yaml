schema_version: "1"
id: overdue-followups-positive
capability: find_overdue_followups
input: {as_of: "2026-01-15"}
fixtures:
  runtime_context:
    granted_scopes: [followup.read]
    tenant_id: tenant-a
expected_calls:
  - operation: crm.find_overdue_followups
    arguments: {as_of: "2026-01-15", tenant_id: tenant-a}
expected_output_schema:
  type: array
  minItems: 1
  items: {type: object}
forbidden_fields: [tenant_id]
