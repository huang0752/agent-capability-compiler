schema_version: "1"
id: search-customers-positive
capability: search_customers
input: {query: acme}
fixtures:
  runtime_context:
    granted_scopes: [customer.read]
    tenant_id: tenant-a
expected_calls:
  - operation: crm.search_customers
    arguments: {query: acme, tenant_id: tenant-a}
expected_output_schema:
  type: array
  minItems: 1
  items: {type: object}
forbidden_fields: [tenant_id]
